/* Page /atelier — parcours expérientiel complet, orchestré côté client.
   V1 : données 100 % simulées (voir lib/atelierApi.ts). */

export default function AtelierPage() {
  return (
    <main className="at-main">
      {/* Squelette du scaffold — remplacé par <AtelierExperience /> */}
      <section className="at-section">
        <p className="at-label">Expérience gratuite — 2 minutes</p>
        <h1 className="at-title">L’atelier Bellajour</h1>
      </section>
    </main>
  )
}
