# 29/08/2026

**Refonte de la structure documentaire et mise en place du système de travail.**
Branche `chantier/structure-produit`.

- L'ancien `CLAUDE.md` (731 lignes, chargé à chaque message) est archivé et remplacé par un socle
  de 84 lignes plus six `CLAUDE.md` de dossier, qui ne se chargent qu'à l'ouverture du dossier
  concerné. Mécanique vérifiée dans la documentation officielle : les `CLAUDE.md` imbriqués sont
  chargés à la demande, les imports `@chemin` non (ils sont eager, ils n'auraient rien économisé).
- Audit de véracité de l'ancien fichier : les six sections de référence rapide étaient fausses
  toutes les six. Les tokens `--cream`/`--steel`/`--ink` documentés comme la charte n'existent
  plus ; la vraie palette est `tokens.css` + `theme.css`, jamais documentée jusqu'ici.
- Cinq sous-agents (`verificateur`, `front`, `backend`, `recetteur`, `gardien`) et quatre
  commandes (`/ticket`, `/travailler`, `/point`, `/recette`).
- Deux garde-fous automatiques : aucun commit ni push sur `main`, aucun `git add -A` à la racine
  (D4). Testés, ils refusent bien.
- Backlog semé avec 30 tickets, tous prouvés dans le code, avec chemin et ligne.

**Trouvé au passage, et c'est le plus important** : la migration `20260829_atelier_tracking_code`
n'est pas appliquée en production. Le repli 42703 fait disparaître le champ du patch en silence,
donc le numéro de suivi d'un colis n'est jamais enregistré. Ticket T-001, bloquant.

**Ce qui reste douteux** : le chiffre de 734 photos orphelines sur R2 (T-023) date et n'a pas été
re-mesuré. Et le sort du parrainage de la prévente (T-002) dépend d'une décision produit que
Mathias n'a pas encore prise.
