# 30/08/2026

Séance de correction, dans la continuité des audits de la veille. Tout est parti en production
sauf le dernier correctif, qui attend un redéploiement.

## Ce qui est fusionné et EN LIGNE

- **PR #9** — les quatre audits (32 constats fichés T-040 à T-071), le socle de vérification
  (scripts npm + workflow GitHub), `.env.example` enfin versionné, l'écran noir sans JavaScript,
  les tokens sortis des journaux, les vignettes supprimées avec leur photo.
- **PR #10** — les titres du récit s'affichent sans JavaScript. Mon correctif de la veille était
  **incomplet** : il ne couvrait que les éléments porteurs de `data-t`, alors que les titres des
  sept pages sont composés ligne à ligne par `.ligne .in`. Signalé par Mathias sur la production.

## Ce qui est fusionné mais PAS déployé

- **PR #11** — le compteur « 12 480 photos » affichait `0` sans JavaScript. Fusionné à 18h38 UTC,
  **aucun déploiement créé par Vercel** douze minutes plus tard. Voir `ETAT-PRODUCTION.md`, qui
  porte la marche à suivre. **C'est le premier geste de la prochaine séance.**

## Les trois bloquants traités

- **T-001** — migration `tracking_code` appliquée en production, colonne vérifiée. Et les deux
  replis `42703` journalisent désormais leur déclenchement : muets, ils transformaient une panne
  bruyante en donnée perdue.
- **T-040** — une adresse déjà connue ne se promeut plus en ambassadrice depuis le formulaire.
  J'ai dû corriger mon propre cadrage : le code documentait le risque de LECTURE et l'assumait ;
  le défaut était l'ÉCRITURE, une signature de charte posée sur la ligne de quelqu'un d'autre.
- **T-002** — **annulé après vérification**, et c'est le plus instructif. `/inviter` est la page
  de la MARRAINE, pas de la filleule. Aucune page n'accueille une filleule, et l'Atelier n'a
  aucun mécanisme de parrainage : `numeros` ne porte aucun champ de rattachement. Le programme
  est honoré, mais sa page d'arrivée et son tunnel n'existent plus. **Décision produit en attente.**

## Ce que la base a appris

Quatre dossiers, tous à `photos_recues`. La chaîne inscription → base → dépôt → admin est
**prouvée sur du réel** : les compteurs, les lignes `photos` et les objets R2 disent le même
nombre, les événements sont journalisés, les mails partent selon la règle.
**La seconde moitié du tunnel n'a jamais tourné** : zéro CGV acceptée, zéro session Stripe, zéro
aperçu, zéro validation. Prouvée en sandbox le 26/08, jamais en production.

Et un fait qui explique tout : **l'atelier ne compose pas encore.** Les clientes s'inscrivent en
avance et sont prévenues à la main. Sans ce fait écrit, chaque relecture de la base rejoue la
même fausse alerte.

## Quatre pièges de méthode, payés aujourd'hui

1. **Le cache de Vercel m'a fait croire qu'un correctif n'était pas passé.** Deux mesures à zéro,
   puis dix avec le cache contourné. J'avais écrit ce piège dans le RUNBOOK le matin même.
2. **Un test qui cherchait `12` dans une ligne contenant déjà `12480`** aurait déclaré la
   réussite quoi qu'il arrive.
3. **`getComputedStyle` lu pendant une transition** rend la valeur intermédiaire : j'ai conclu à
   tort que `/magazine` ne suivait pas le filet.
4. **Mon propre garde-fou a crié à tort deux fois**, et ma première correction avait
   silencieusement RE-AUTORISÉ `git push origin main`. Trouvé en testant. Il a désormais sa
   batterie de onze cas : `./.claude/hooks/test-garde-main.sh`.

Le fil commun : sur ce projet, **une vérification hâtive ment plus souvent qu'elle n'informe.**

## À reprendre, dans l'ordre

1. Redéployer (voir `ETAT-PRODUCTION.md`), puis vérifier le compteur.
2. Trancher T-002 — la seule décision produit qui bloque.
3. Éprouver ce qui n'a jamais tourné : une expédition réelle avec son suivi, et le parcours
   ambassadeur complet formulaire → mail → clic.

Backlog : **71 tickets, 16 fermés, 55 ouverts** — 1 bloquant (T-002), 42 sérieux, 12 confort.
